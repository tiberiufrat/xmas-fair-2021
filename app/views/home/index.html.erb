<div class="header bg-<%= user_signed_in? ? current_user.theme || 'primary' : 'primary' %> pb-6">
  <div class="container-fluid">
    <div class="header-body">
      <%= render partial: 'shared/header', locals: {
            icon: 'far fa-home-lg',
            breadcrumb_1: 'Dashboards',
            breadcrumb_2: 'Default'
      } %>
      <%= render 'shared/dashboard/metrics_summary' %>
    </div>
  </div>
</div>
<div class="container-fluid mt--6">
  <div class="row">
    <div class="col-xl-8">
      <%= render 'shared/dashboard/charts/sales_copy' %>
    </div>
    <div class="col-xl-4">
      <%= render 'shared/dashboard/charts/orders' %>
    </div>
  </div>
  <div class="row">
    <div class="col-xl-8">
      <%= render 'shared/dashboard/tables/page_visits' %>
    </div>
    <div class="col-xl-4">
      <%= render 'shared/dashboard/tables/social_traffic' %>
    </div>
  </div>
</div>

<% content_for(:page_title, 'Dashboard') %>
